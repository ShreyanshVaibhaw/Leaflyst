import { Panel } from "@/components/ui";
import type { ImpactPreview } from "@/lib/api";

export function RevocationPanel({ impact, action }: { impact: ImpactPreview; action: (form: FormData) => Promise<void> }) {
  return <Panel className={`p-6 ${impact.cold ? "border-emerald-200" : "border-amber-200"}`}><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold">Containment</h2><p className="mt-1 text-xs text-slate-500">Always preceded by an impact check</p></div><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold uppercase ${impact.cold ? "bg-emerald-100 text-emerald-800" : "bg-amber-100 text-amber-800"}`}>{impact.cold ? "Provably cold" : "Warm credential"}</span></div><dl className="mt-5 grid grid-cols-2 gap-3 text-sm"><div><dt className="text-xs text-slate-500">Recent events</dt><dd className="mt-1 font-semibold">{impact.events_last_30d}</dd></div><div><dt className="text-xs text-slate-500">Known consumers</dt><dd className="mt-1 font-semibold">{impact.agent_consumers.length}</dd></div><div><dt className="text-xs text-slate-500">Reachable resources</dt><dd className="mt-1 font-semibold">{impact.reachable_resources.length}</dd></div><div><dt className="text-xs text-slate-500">Write credential</dt><dd className="mt-1 font-semibold">{impact.write_credential_configured ? "Separate scope ready" : "Not configured"}</dd></div></dl>{impact.one_click ? <form action={action} className="mt-5 space-y-3"><input type="hidden" name="action" value={impact.next_action} /><label className="block text-xs font-medium text-slate-600">Type <span className="font-mono">{impact.fingerprint}</span> to confirm</label><input required name="confirmation" autoComplete="off" className="w-full rounded-lg border border-slate-300 px-3 py-2 font-mono text-xs" /><button className="w-full rounded-lg bg-rose-700 px-4 py-2 text-sm font-semibold text-white">{impact.next_action === "deactivate" ? "Deactivate key" : "Revoke credential"}</button></form> : <div className="mt-5"><p className="text-sm leading-6 text-amber-800">One-click containment is disabled. Rotate known consumers, then use the guided commands.</p><div className="mt-3 space-y-2">{impact.guided_commands.map((command) => <pre key={command} className="overflow-auto rounded-lg bg-slate-950 p-3 text-[11px] text-slate-100">{command}</pre>)}</div></div>}</Panel>;
}
