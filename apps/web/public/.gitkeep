# Keeps apps/web/public/ present in a fresh checkout.
# infra/docker/web.Dockerfile copies /app/public; git does not track empty
# directories, so without this file the container build fails on a clean clone.
